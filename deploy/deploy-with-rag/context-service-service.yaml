apiVersion: v1
kind: Service
metadata:
  name: context-service
  namespace: orange-code
  labels:
    app: context-service
    component: rag
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: context-service