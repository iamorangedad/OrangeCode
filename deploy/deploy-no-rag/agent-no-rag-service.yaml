# Service omitted - using kubectl exec for interactive access
# Uncomment if you need internal service discovery or future HTTP endpoint
#
# apiVersion: v1
# kind: Service
# metadata:
#   name: agent-no-rag
#   namespace: orange-code
#   labels:
#     app: agent-no-rag
#     component: agent
# spec:
#   type: ClusterIP
#   ports:
#   - port: 8080
#     targetPort: 8080
#     protocol: TCP
#   selector:
#     app: agent-no-rag